fastapi>=0.111
uvicorn[standard]>=0.30
requests>=2.32
beautifulsoup4>=4.12
# Быстрый HTML-парсер: за цикл разбираются десятки многомегабайтных
# снимков DOM, html.parser на слабом VPS не успевает
lxml>=5.0

# Нужен для динамических сайтов (Winline / Лига Ставок).
# Требует установленного Chromium/Chrome в системе (см. README).
selenium>=4.21

# BetBoom собирается через прямой websocket-фид линии (protobuf вручную),
# браузер для него больше не нужен — только клиент websocket.
websocket-client>=1.7
