fastapi>=0.111
uvicorn[standard]>=0.30
requests>=2.32
beautifulsoup4>=4.12
# Быстрый HTML-парсер: за цикл разбираются десятки многомегабайтных
# снимков DOM, html.parser на слабом VPS не успевает
lxml>=5.0

# Нужен для динамических сайтов (Лига Ставок).
# Требует установленного Chromium/Chrome в системе (см. README).
selenium>=4.21

# Winline и BetBoom собираются через прямые websocket-фиды линии
# (бинарные протоколы разобраны вручную), браузер им не нужен —
# только клиент websocket.
websocket-client>=1.7

# SOCKS-прокси для requests (LIGASTAVOK_PROXY / STAKE_PROXY=socks5://...):
# Qrator Лиги Ставок и Cloudflare Stake блокируют IP дата-центров, нужен
# резидентный прокси. Он же нужен локальному ретранслятору прокси для
# браузера (app/proxy_relay.py).
pysocks>=1.7

# Stake: челлендж Cloudflare (Turnstile) проходит только «настоящий»
# Chrome без следов автоматизации — обычный chromedriver Cloudflare
# распознаёт и галочку не засчитывает (проверено). Нужен Chrome и экран
# (DISPLAY или Xvfb — парсер поднимет Xvfb сам, если он установлен).
undetected-chromedriver>=3.5

# Шифрование логинов/паролей аккаунтов БК в базе (админка) и подпись
# сессионных cookie.
cryptography>=41.0
pydantic>=2.0
